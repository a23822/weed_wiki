import React from 'react';

//scss
import styles from './flickingButton.module.scss';

const flickingButton = (props) => {

    return (
        <div className={styles.flick_btn_area}>
            테스트
        </div>
    )
}

export default flickingButton;